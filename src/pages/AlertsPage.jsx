import React from "react";
import AlertLogList from "../components/Alerts/AlertLogList"; // Adjust path if different

const AlertsPage = () => {
  return (
    <div>
      <AlertLogList />
    </div>
  );
};

export default AlertsPage;
